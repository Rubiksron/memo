<!DOCTYPE html>
<html lang="en" style="display: none;">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <form action="/" style="display: none;">
    <button id="userIdEjs"></button>
  </form>  
  
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
  <script>
    var id = <%= userId %>;
      if(localStorage.choreBearUser) {
        const userObjParsed = JSON.parse(localStorage.choreBearUser);
        userObjParsed.id = id;
        const userObjStringified = JSON.stringify(userObjParsed);
        localStorage.setItem('choreBearUser', userObjStringified);
        document.getElementById('userIdEjs').click();
      }s
      const userObjParsed = JSON.parse(localStorage.choreBearUser);
      console.log('userId.ejs - userObjParsed: ', userObjParsed);
  </script>
</body>
</html>