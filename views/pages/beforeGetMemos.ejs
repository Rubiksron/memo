<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>

  <form action="/getMemos" method="POST">
    <button id="beforeGetMemosButton"></button>
    <input  type="input" name="idInput" id="idInput">
  </form>  

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
  <script>
      if(localStorage.choreBearUser) {
        const userObjParsed = JSON.parse(localStorage.choreBearUser);
        $('#idInput').val(userObjParsed.id);
      }
      document.getElementById('beforeGetMemosButton').click();
      alert('stop here beforeGetMemos.ejs');
  </script>
</body>
</html>